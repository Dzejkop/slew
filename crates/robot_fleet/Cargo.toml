[package]
name = "robot_fleet"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A TUI robot-fleet game built on the slew interpreter"
publish = false

[dependencies]
slew = { path = "../slew", default-features = false }
ratatui = "0.30.2"

[lints.rust]
unsafe_code = "forbid"
